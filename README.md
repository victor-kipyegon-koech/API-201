1. initialize a project:
    pnpm init- package.json
2. install the packages
   pnpm i express(web server) ,pg (PostgreSQL client driver),dotenv(environment variables) ,cors(cross-origin-access), helmet(security headers) ,express-rate-limit(brute-force protection),
      express-async-handler(cleans up async/await try-catch blocks)
